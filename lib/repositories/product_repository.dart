class ProductRepository {
  // Placeholder for future product/transaction data fetching
  Future<List<Map<String, dynamic>>> fetchTransactions() async {
    await Future.delayed(const Duration(milliseconds: 300));
    return [];
  }
}
